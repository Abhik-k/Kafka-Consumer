server:
  port: 9292

spring:
  kafka:
    consumer:
      bootstrap-server: localhost:9092
      group-id: jt-group
      properties:
        spring:
          json:
            trusted:
              packages: com.kafka.kafkaconsumer.dto
